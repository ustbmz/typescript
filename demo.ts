interface Person { name: string, age: number }

const teacher: Person = {
    name :'userTest',age:16
}

console.log(teacher);


// 基本类型 number string boolean symbol null undfined void
// 对象类型 {} [] function class

// 类型注解 type annotation  定义变量 带类型
// 类型推断 type inference   TS自动推断中变量类型

// 能推断就推断，无法推断使用类型注解




